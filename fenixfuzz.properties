# positive integer
minimum = 1
# integer greater than minimum
maxium = 20
# either "generation" or "mutation"
genmode = generation
# 1 if the API is to be tested, 0 if not
test_api = 0
# Used to build the API URL (prolly gonna change to a full URL)
api_version = 1
# Either "all", "no-white", "alpha", "char" or "num"
charset = all
# Either "student", "teacher" or "sysadm"
user = student
# E.g.: "/path/to/file/example.json"
crawl_exclude_urls = exclude.json
# E.g.: "/path/to/file/example.ffm"
fenixfuzz_model = ffm.json
# E.g.: http://localhost:8080/<local_url>
local_url = starfleet